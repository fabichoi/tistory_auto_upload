name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [ push ]
jobs:
  initial-settings:
    runs-on: ubuntu-latest
    steps:
      - name: make dotenv file
        run: |          
          echo "BLOG_NAME=${{ secrets.BLOG_NAME }}" >> .env
      - name: check dotenv var
        run: |
          echo "cat .env"
